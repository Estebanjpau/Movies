import 'package:flutter/material.dart';

@immutable
class MoviesColors {

  const MoviesColors._();

  // Foundation
  static const Color white = Color(0xFFF2F2F2);
  static const Color whiteMuted = Color(0x6EFFFFFF);
  static const Color black = Color(0xFF0B0B0B);
  static const Color blackMuted = Color(0x6E000000);
  static const Color transparent = Color(0x0);

  //Brand
  static const Color primaryYellow = Color(0xFFFFC700);

  //grey
  static const Color primaryGray = Color(0xFF555555);
  static const Color gray = Color(0xFF999999);
  static const Color mutedGray = Color(0xFF2B2B2B);
}
